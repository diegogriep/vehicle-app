'use client'

import styled from 'styled-components'

export const Wrapper = styled.main`
  color: black;
  padding: 2rem 1rem;
  max-width: 122.4rem;
  margin-left: auto;
  margin-right: auto;
`

export const Title = styled.h1`
  color: green;
`
